apiVersion: v1
kind: Service
metadata:
  name: {{ include "nsrl-server.fullname" . }}
  labels:
    {{- include "nsrl-server.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "nsrl-server.selectorLabels" . | nindent 4 }}
